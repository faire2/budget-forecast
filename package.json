{
  "name": "budget-forecast",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace budget-forecast-server dev\" \"yarn workspace budget-forecast-client dev\"",
    "build": "yarn workspace budget-forecast-server build && yarn workspace budget-forecast-client build",
    "lint": "yarn workspaces run lint",
    "type-check": "yarn workspaces run type-check",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,css,md}\""
  },
  "dependencies": {
    "drizzle-orm": "^0.30.0",
    "@neondatabase/serverless": "^0.9.0",
    "zod": "^3.22.0",
    "date-fns": "^3.3.0"
  },
  "devDependencies": {
    "@vercel/node": "^5.6.22",
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  }
}
